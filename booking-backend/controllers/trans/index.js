const { getTrans } = require('./getTrans');
const { createTrans } = require('./createTrans');
const { deleteTrans } = require('./deleteTrans');

module.exports = {
    getTrans,
    createTrans,
    deleteTrans,
};
